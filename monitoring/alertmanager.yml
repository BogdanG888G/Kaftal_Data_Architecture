global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'telegram'

receivers:
  - name: 'telegram'
    telegram_configs:
      - bot_token: '8421267289:AAFPHO71a7vWJlDBFVBDMy6cHNAXbqJP2FI'
        chat_id: 	433923554
        message: |
          🚨 {{ .CommonLabels.alertname }}
          {{ .CommonAnnotations.summary }}
          {{ .CommonAnnotations.description }}